# =========================================
# EVM IPFS UPLOAD - CENTRALIZED ENVIRONMENT
# =========================================
# Single .env file for the entire project
# This file should be copied and renamed from .env.example to .env
# and kept in .gitignore

# =========================================
# MONGODB CONFIGURATION
# =========================================
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=change_this_password_123

# MongoDB URI for local development (no auth)
# MONGO_URI=mongodb://localhost:27017/ipfs-upload

# MongoDB URI for Docker or with authentication
MONGO_URI=mongodb://admin:change_this_password_123@localhost:27017/ipfs-upload?authSource=admin

# =========================================
# BACKEND SERVER CONFIGURATION
# =========================================
PORT=5001
NODE_ENV=development

# =========================================
# PINATA IPFS CONFIGURATION (REQUIRED)
# =========================================
# Get your keys at: https://pinata.cloud
PINATA_API_KEY=your_pinata_api_key_here
PINATA_SECRET_KEY=your_pinata_secret_key_here
PINATA_GATEWAY_URL=https://gateway.pinata.cloud

# Pinata Upload Settings (Optional - defaults shown)
PINATA_TIMEOUT=60000
PINATA_MAX_RETRIES=2
MAX_FILE_SIZE=104857600

# =========================================
# BACKEND SECURITY CONFIGURATION
# =========================================
# CORS - Comma-separated allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# =========================================
# ETHEREUM NETWORK CONFIGURATION
# =========================================
# Network for explorer URLs (mainnet, goerli, sepolia, etc.)
ETH_NETWORK=mainnet

# =========================================
# FRONTEND (REACT) CONFIGURATION
# =========================================
# Backend Server URL
REACT_APP_SERVER_URL=http://localhost:5001

# IPFS Gateway URL for viewing files
REACT_APP_PINATA_GATEWAY_URL=https://gateway.pinata.cloud

# =========================================
# DOCKER-SPECIFIC OVERRIDES
# =========================================
# Uncomment these when running with Docker Compose:
# MONGO_URI=mongodb://admin:change_this_password_123@mongodb:27017/ipfs-upload?authSource=admin
# REACT_APP_SERVER_URL=http://backend:5001

# =========================================
# SETUP INSTRUCTIONS
# =========================================
# 1. Copy this file to .env in the project root:
#    cp .env.example .env
#
# 2. Update PINATA_API_KEY and PINATA_SECRET_KEY with your actual values
#
# 3. Change MONGO_INITDB_ROOT_PASSWORD to a strong password
#
# 4. For Docker deployment:
#    - Uncomment the Docker-specific MONGO_URI
#    - Update docker-compose.yml to use this file
#
# 5. Never commit .env file to version control!

# =========================================
# LEGACY NOTICE
# =========================================
# This replaces the following files:
# - app/.env (deprecated - delete after migration)
# - server/.env (deprecated - delete after migration)
# - .env.docker (deprecated - delete after migration)

